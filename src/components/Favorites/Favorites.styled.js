import styled from 'styled-components';

export const ContainerText = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  height: 500px;

  & > p {
    font-size: 50px;
    font-weight: 500;
    font-family: 'Manrope';
    color: black;
  }
`;
